(function (window) {
  window.__env = window.__env || {};
  // Copia este archivo como env.js y ajusta la URL para desarrollo local si es necesario.
  // URL por defecto
  window.__env.apiUrl = 'http://localhost:8080';
})(this);
